Intentionally empty. Propose the schema and Row Level Security policies in plain English
first (see CLAUDE.md "Data and security"), get approval, then create migrations here.
Needed tables at minimum: articles, photos, intake_submissions, csa_acceptances.
The last two must default-deny public/anonymous reads.
